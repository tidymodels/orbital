% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate-size.R
\name{estimate_orbital_size}
\alias{estimate_orbital_size}
\alias{estimate_orbital_size.xgb.Booster}
\alias{estimate_orbital_size.lgb.Booster}
\alias{estimate_orbital_size.ranger}
\alias{estimate_orbital_size.randomForest}
\alias{estimate_orbital_size.rpart}
\alias{estimate_orbital_size.constparty}
\alias{estimate_orbital_size.catboost.Model}
\alias{estimate_orbital_size.glm}
\alias{estimate_orbital_size.lm}
\alias{estimate_orbital_size.glmnet}
\alias{estimate_orbital_size.earth}
\alias{estimate_orbital_size.recipe}
\alias{estimate_orbital_size.workflow}
\alias{estimate_orbital_size.tailor}
\title{Estimate orbital expression character count}
\usage{
estimate_orbital_size(x, ...)

\method{estimate_orbital_size}{xgb.Booster}(x, ...)

\method{estimate_orbital_size}{lgb.Booster}(x, ...)

\method{estimate_orbital_size}{ranger}(x, ...)

\method{estimate_orbital_size}{randomForest}(x, ...)

\method{estimate_orbital_size}{rpart}(x, ...)

\method{estimate_orbital_size}{constparty}(x, ...)

\method{estimate_orbital_size}{catboost.Model}(x, ...)

\method{estimate_orbital_size}{glm}(x, ...)

\method{estimate_orbital_size}{lm}(x, ...)

\method{estimate_orbital_size}{glmnet}(x, ..., penalty = NULL)

\method{estimate_orbital_size}{earth}(x, ...)

\method{estimate_orbital_size}{recipe}(x, ...)

\method{estimate_orbital_size}{workflow}(x, ...)

\method{estimate_orbital_size}{tailor}(x, ...)
}
\arguments{
\item{x}{A fitted model object, workflow, prepped recipe, or fitted tailor.}

\item{...}{Additional arguments passed to methods.}

\item{penalty}{For glmnet models, the penalty value (lambda) to use. If the
model was fit with a single lambda, this is used by default. Otherwise,
you must specify a value.}
}
\value{
An integer estimate of the total character count of the orbital
expression.
}
\description{
Estimates the character count of the orbital expression that would be
generated for a model, without actually generating it. This is useful during
hyperparameter tuning when you want to track SQL size as a metric but don't
want to pay the cost of generating the full orbital object for every
candidate model.
}
\details{
The estimation uses model metadata (tree structure, number of parameters,
feature names) to approximate the size of the resulting orbital expression.
The estimates are typically within 5-10\% of the actual size.

For tree-based models, this function is much faster than generating the full
orbital object because it only needs to inspect the tree structure, not
convert each tree to an R expression.

This function aims to support all the same models and preprocessing
operations as \code{\link[=orbital]{orbital()}}. If you find a case where \code{orbital()} works but
\code{estimate_orbital_size()} does not, please
\href{https://github.com/tidymodels/orbital/issues}{file an issue}.
}
\examples{
\dontshow{if (rlang::is_installed("xgboost")) withAutoprint(\{ # examplesIf}
library(xgboost)

# Estimate size for an xgboost model
x <- as.matrix(mtcars[, -1])
y <- mtcars[, 1]
model <- xgboost(x = x, y = y, nrounds = 50, max_depth = 4, verbosity = 0)

estimate_orbital_size(model)
\dontshow{\}) # examplesIf}
\dontshow{if (rlang::is_installed(c("recipes", "workflows", "parsnip"))) withAutoprint(\{ # examplesIf}
library(recipes)
library(workflows)
library(parsnip)

# Estimate size for a workflow
rec <- recipe(mpg ~ ., data = mtcars) |>
  step_normalize(all_numeric_predictors())

wf <- workflow() |>
  add_recipe(rec) |>
  add_model(linear_reg()) |>
  fit(mtcars)

estimate_orbital_size(wf)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=orbital]{orbital()}} for generating orbital objects.
}
