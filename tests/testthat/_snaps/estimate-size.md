# estimate_orbital_size errors for unsupported types

    Code
      estimate_orbital_size(lm(mpg ~ ., mtcars))
    Condition
      Error in `estimate_orbital_size()`:
      ! `estimate_orbital_size()` is not implemented for a <lm> object.

