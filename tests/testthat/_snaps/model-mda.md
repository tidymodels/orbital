# discrim_flexible(earth) is refused

    Code
      orbital(fit, type = "class")
    Condition
      Error in `parse_model()`:
      ! The `method` used to fit this mda model is not supported.
      i Only `mda::polyreg()` and `mda::gen.ridge()` are supported, not <earth>.

